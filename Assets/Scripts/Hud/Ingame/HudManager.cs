using UnityEngine;
using System.Collections;

// This class will use alot of helper classes to manage the hud.
// It is through this class that button clicks will go to access the correct functions
public class HudManager : MonoBehaviour {
    // Used to build buttons in the ingame hud
    private ButtonBuilder m_buttonBuilder;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
